# Optional local release configuration.
# Copy to .env only if your own shell workflow loads it. The scripts read the
# environment directly and do not require this file.

TEAM_ID=ABCDE12345
APPLE_ID=you@example.com
NOTARY_PROFILE=WhatsAppConnector-notary
BUNDLE_ID=app.whatsappconnector.mac
